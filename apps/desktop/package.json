{
  "name": "@sve/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Windows desktop shell (Tauri): window lifecycle, native command registration, packaging.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
